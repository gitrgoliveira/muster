run:
  timeout: 3m
  go: "1.26"

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - gofmt
    - gosimple
    - ineffassign
    - staticcheck
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters: [errcheck]
